#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns length of a string
 * @s: character
 * Return: returns the length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
