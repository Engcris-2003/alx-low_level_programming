#include <stdio.h>
#include <ctype.h>
/**
 * main - main block
 * Description: prints alphabets in lowercase and uppercase
 *
 * Return: 0
 *
 */
int main(void)
{
	char ch;
	for (ch = 'a'; char = 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
