#include "main.h"
/**
 * _islower - writes a function that checks lowercase character
 * @c: a character argument
 * Return: returns 0 or 1 depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
