#include "main.h"
int check_prime_number(int );
/**
 * is_prime_number - finds prime number
 * @n: interger
 * Return: 1 or 0
 */
int is_prime_number(int n)
{
	return (is_prime_number(n, 2);
}
/**
 * check_prime_number - finds a prime number
 * @n: integer
 * Return: prime number
 */
int check_prime_number(int )
{
	
}
